-- local parent = Settings.RegisterVerticalLayoutCategory('Scavenger')
-- local child = Settings.RegisterVerticalLayoutSubcategory(parent, 'Lootable items')
-- local child2 = Settings.RegisterVerticalLayoutSubcategory(parent, 'Ignored items')

-- Settings.RegisterAddOnCategory(parent)

-- local setting = Settings.RegisterProxySetting(
--   parent, 'test', Settings.VarType.Boolean, 'Test', true,
--   function() return true end, function() backbone.print('Triggered!') end
-- )

-- Settings.CreateCheckbox(parent, setting)
